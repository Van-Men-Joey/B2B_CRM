@page
@model Customer_Relationship_Management.Pages.Employee.Support.IndexModel
@{
    Layout = "/Pages/Shared/_LayoutEmployee.cshtml";
    ViewData["Title"] = "Support";
    ViewData["ActiveMenu"] = "Support";
}

<div class="container-fluid py-4 px-3 animate__animated animate__fadeIn">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold text-gradient mb-0">Trung tâm Hỗ trợ</h2>
            <p class="text-muted mb-0 small">Tính năng đang được phát triển và sẽ sớm ra mắt</p>
        </div>
    </div>

    <!-- Coming Soon Card -->
    <div class="card border-0 shadow-lg glass-card">
        <div class="card-header bg-gradient-blue text-white d-flex align-items-center gap-2">
            <i class="bi bi-tools fs-5"></i>
            <h5 class="mb-0 fw-semibold">Sắp ra mắt</h5>
        </div>
        <div class="card-body py-5">
            <div class="text-center">
                <div class="mb-3">
                    <i class="bi bi-life-preserver" style="font-size:64px;color:#0d6efd"></i>
                </div>
                <h4 class="fw-bold mb-2">Chức năng Hỗ trợ sẽ được cung cấp sau</h4>
                <p class="text-muted mb-4">Chúng tôi đang xây dựng module Ticket/Helpdesk để bạn có thể tạo yêu cầu hỗ trợ, theo dõi trạng thái và trao đổi với đội kỹ thuật.</p>

                <div class="d-flex justify-content-center gap-2 flex-wrap">
                    <a asp-page="/Employee/Dashboard" class="btn btn-gradient px-4">Quay lại Dashboard</a>
                    <a asp-page="/Employee/Customer/Customer" class="btn btn-outline-secondary px-4">Xem khách hàng</a>
                    <button type="button" class="btn btn-secondary px-4" disabled title="Tính năng sẽ sớm mở">Tạo ticket hỗ trợ</button>
                </div>
            </div>
        </div>
        <div class="card-footer bg-transparent text-center small text-muted">
            Có góp ý cho tính năng này? Hãy liên hệ quản lý của bạn để được hỗ trợ.
        </div>
    </div>
</div>

@section Styles {
    <style>
        .btn-gradient {
            background: linear-gradient(45deg, #007bff, #00d4ff);
            color: #fff;
            border: none;
            transition: .3s;
        }

            .btn-gradient:hover {
                transform: scale(1.05);
                opacity: .9;
            }

        .text-gradient {
            background: linear-gradient(90deg, #007bff, #00c6ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .glass-card {
            backdrop-filter: blur(10px);
            background: rgba(255, 255, 255, 0.9);
            border-radius: 14px;
            color: #212529;
        }

        .bg-gradient-blue {
            background: linear-gradient(45deg, #007bff, #00b4ff);
        }
    </style>
}
