@page
@model Customer_Relationship_Management.Pages.Manager.Support.IndexModel
@{
    Layout = "_LayoutManager";
    ViewData["Title"] = "Support";
    ViewData["ActiveMenu"] = "Support";
}

<div class="container-fluid py-4 px-3 animate__animated animate__fadeIn">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold text-gradient-mgr mb-0">Trung tâm Hỗ trợ</h2>
            <p class="text-muted mb-0 small">Tính năng đang được phát triển và sẽ sớm ra mắt</p>
        </div>
    </div>

    <!-- Coming Soon Card -->
    <div class="card border-0 shadow-lg glass-card">
        <div class="card-header bg-gradient-mgr text-white d-flex align-items-center gap-2">
            <i class="bi bi-tools fs-5"></i>
            <h5 class="mb-0 fw-semibold">Sắp ra mắt</h5>
        </div>
        <div class="card-body py-5">
            <div class="text-center">
                <div class="mb-3">
                    <i class="bi bi-life-preserver" style="font-size:64px;color:#198754"></i>
                </div>
                <h4 class="fw-bold mb-2">Chức năng Hỗ trợ sẽ được cung cấp sau</h4>
                <p class="text-muted mb-4">Chúng tôi đang xây dựng module Ticket/Helpdesk để bạn có thể tạo yêu cầu hỗ trợ, theo dõi trạng thái và trao đổi với đội kỹ thuật.</p>

                <div class="d-flex justify-content-center gap-2 flex-wrap">
                    <a asp-page="/Manager/Index" class="btn btn-gradient-mgr px-4">Về Dashboard</a>
                    <a asp-page="/Manager/Customer/Index" class="btn btn-outline-secondary px-4">Xem khách hàng</a>
                    <button type="button" class="btn btn-secondary px-4" disabled title="Tính năng sẽ sớm mở">Tạo ticket hỗ trợ</button>
                </div>
            </div>
        </div>
        <div class="card-footer bg-transparent text-center small text-muted">
            Có góp ý cho tính năng này? Vui lòng liên hệ bộ phận quản trị hoặc admin.
        </div>
    </div>
</div>

@section Styles {
    <style>
        .btn-gradient-mgr {
            background: linear-gradient(180deg,#198754 0%,#20c997 100%);
            color: #fff;
            border: none;
            transition: .3s;
        }

            .btn-gradient-mgr:hover {
                transform: translateY(-1px);
                opacity: .95;
            }

        .text-gradient-mgr {
            background: linear-gradient(180deg,#198754 0%,#20c997 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .glass-card {
            backdrop-filter: blur(10px);
            background: rgba(255,255,255,.92);
            border-radius: 14px;
            color: #212529;
        }

        .bg-gradient-mgr {
            background: linear-gradient(180deg,#198754 0%,#20c997 100%);
        }
    </style>
}